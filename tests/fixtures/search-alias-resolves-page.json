{
  "_comment": "Index-side alias resolution: searching a page's multi-word alias (Markov chain Monte Carlo) resolves to its slug (mcmc) via the vault label FTS index, even though no observation body contains that phrase. Exact alias hits are promoted ahead of content hits.",
  "cmd": ["search", "Markov chain Monte Carlo", "--explain"]
}
