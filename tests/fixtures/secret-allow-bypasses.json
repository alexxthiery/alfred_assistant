{
  "_comment": "--allow-secret bypasses the contains-secret rule only. Write lands; log + commit subject redact the value (verified live, the fixture only locks exit 0).",
  "cmd": ["write", "leak-bypass", "--title", "Leak bypass", "--type", "concept", "--tags", "meta", "--allow-secret", "--content", "- [fact] sk-AbCdEfGhIjKlMnOpQrSt1234 test value ^[t:1]"]
}
